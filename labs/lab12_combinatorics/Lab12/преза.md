


### Код алгоритма 

```cpp
№1  #include <iostream>
№2  using namespace std;

№3  int *P, *R, n;

№4  void per(int k) {
№5      for (int i = 1; i <= n; i++) {
№6          if (R[i] == 0) { 
№7              P[k] = i;
№8              R[i] = 1;
№9              if (k == n) { 
№10                 for (int j = 1; j <= n; j++) cout << P[j] << " ";
№11                 cout << endl;
№12             } else per(k + 1);          
№13             R[i] = 0;          
№14         }
№15     }
№16 }

№17 int main() {
№18     n = 3;
№19     P = new int[n + 1]; 
№20     R = new int[n + 1]{0};
№21     per(1);
№22     return 0;
№23 }
```


### Cлайд 1: Инициализация в main()
Задаем `n = 3`. Выделяем массивы

```cpp
№18     n = 3;
№19     P = new int[n + 1]; 
№20     R = new int[n + 1]{0};
```

| Статическая память | Динамическая память |
| :--- | :--- | 
| **R** = (адрес) | **R** = [0, 0, 0] | 
| **P** = (адрес) | **P** = [?, ?, ?] | 

---

### Слайд 2: Выбор первого числа
Вызываем `per(1)`. В стек попадает вызов функции с параметром `k = 1`. Первое свободное число `i = 1`.

```cpp
№21     per(1);
// В per(1)
№5      for (int i = 1; i <= n; i++) { 
№6          if (R[i] == 0) {           // R[1] == 0 (не занято)
№7              P[k] = i;   // P[1] = 1
№8              R[i] = 1;   // R[1] = 1 (занято)
```

| Статическая память | Динамическая память |
| :--- | :--- | 
| **R** = (адрес) | **R** = [1, 0, 0] |
| **P** = (адрес) | **P** =[1, ?, ?] | 

---

### Слайд 3: Выбор второго числа
Вызываем `per(2)`. Для второй позиции число 1 уже занято, поэтому берем следующее свободное число, то есть 2

```cpp
№12             } else per(k + 1);     // Вызов per(2)
// Внутри per(2)
№5      for (int i = 1; i <= n; i++) { // i = 2
№7              P[k] = i;    // P[2] = 2
№8              R[i] = 1;   // R[2] = 1 
```

| Статическая память | Динамическая память |
| :--- | :--- | 
| **R** = (адрес) | **R** =[1, 1, 0] | 
| **P** = (адрес) | **P** = [1, 2, ?] |

---

### Слайд 4: Выбор третьего числа и вывод 
Вызываем `per(3)`. Свободна только тройка. Так как мы заполнили массив (`k == n`), выводим комбинацию на экран.

```cpp
№12             } else per(k + 1);     // вызов per(3)
// Внутри per(3)
№5      for (int i = 1; i <= n; i++) { // i = 3
№7              P[k] = i;    // P[3] = 3
№8              R[i] = 1;   // R[3] = 1 
№9              if (k == n) {          // k=3 
№10                 ... cout << P[j];  // вывод: 1 2 3
```

| Статическая память | Динамическая память |
| :--- | :--- | 
| **R** = (адрес) | **R** = [1, 1, 1] |
| **P** = (адрес) | **P** = [1, 2, 3] |

---
### Слайд 5: Назад
`R[3] = 0`. Цикл в `per(3)` заканчивается. Функция завершается и удаляется

```cpp
№13             R[i] = 0;              // R[3] = 0
№14         }
№15     }                              // Выходим из per(3)
```

| Статическая память | Динамическая память |
| :--- | :--- | 
| **R** = (адрес) | **R** =[1, 1, 0] |
| **P** = (адрес) | **P** =[1, 2, 3] |

---

### Слайд 6: Продолжение перебора
Вернулись в `per(2)`, далее `R[2] = 0`. Цикл делает шаг `i = 3`. Ставим тройку на вторую позицию.

```cpp
// в per(2)
№13             R[i] = 0;              // R[2] = 0 
№5      for (int i = 1; i <= n; i++) { // i = 3
№7              P[k] = i;    // P[2] = 3
№8              R[i] = 1;   // R[3] = 1 
№12             ... else per(k + 1);   // вызов per(3)
```

| Статическая память | Динамическая память |
| :--- | :--- | 
| **R** = (адрес) | **R** =[1, 0, 1] | 
| **P** = (адрес) | **P** = [1, 3, 3] | 

---

### Слайд 7: Переходим в per(3) и вывод второй перестановки
Единица и тройка заняты. берем `i = 2`. Далее выводим результат.

```cpp
// внутри per(3)
№5      for (int i = 1; i <= n; i++) { // i = 2
№7              P[k] = i;  // P[3] = 2
№8              R[i] = 1;   // R[2] = 1 
№9              if (k == n) {          // k == 3
№10                 ... cout << P[j];  // вывод: 1 3 2
```

| Статическая память | Динамическая память |
| :--- | :--- | 
| **R** = (адрес) | **R** = [1, 1, 1] | 
| **P** = (адрес) | **P** =[1, 3, 2] | 

---

### Слайд 8: Удаляем значение 2 и 3 позиции
После вывода 1 3 2: `R[2] = 0`, выходим из `per(3)`. Возвращаемся в `per(2)`: `R[3] = 0`. Цикл в `per(2)` завершен.

```cpp
// в per(3) и в per(2) 
№13             R[i] = 0;                       
```

| Статическая память | Динамическая память |
| :--- | :--- |
| **R** = (адрес) | **R** =[1, 0, 0] |  
| **P** = (адрес) | **P** = [1, 3, 2] |

---

### Слайд 9: Смена элемента в per(1)
Мы оказались в самом начале (`per(1)`). Освобождаем `R[1]`. `i` становится 2.

```cpp
//в per(1)
№13             R[i] = 0;              // R[1] = 0.
№5      for (int i = 1; i <= n; i++) { // i = 2
№7              P[k] = i;    // P[1] = 2
№8              R[i] = 1;   // R[2] = 1 
№12             ... else per(k + 1);   // Вызов per(2)
```

| Статическая память | Динамическая память |
| :--- | :--- | 
| **R** = (адрес) | **R** =[0, 1, 0] | 
| **P** = (адрес) | **P** = [2, 3, 2] | 

---

### Слайд 10: Формирование новой комбинации в per(2)
Так как `i = 1` свободно, ставим на вторую позицию.

```cpp
// в per(2)
№5      for (int i = 1; i <= n; i++) { // i = 1
№6          if (R[i] == 0) {           // R[1] == 0 
№7              P[k] = i;   // P[2] = 1
№8              R[i] = 1;   // R[1] = 1 
№12             ... else per(k + 1);   //  per(3)
```

| Статическая память | Динамическая память |
| :--- | :--- | 
| **R** = (адрес) | **R** =[1, 1, 0] | 
| **P** = (адрес) | **P** = [2, 1, 2] |

---

### Слайд 11: Вывод
Итого свободна только тройка, берем ее.

```cpp
// в per(3)
№5      for (int i = 1; i <= n; i++) { //  i = 3
№7              P[k] = i;    // P[3] = 3
№8              R[i] = 1;   // R[3] = 1 
№9              if (k == n) {          // k ==3 
№10                 ... cout << P[j];  // вывод: 2 1 3
```

| Статическая память | Динамическая память |
| :--- | :--- |
| **R** = (адрес) | **R** =[1, 1, 1] |
| **P** = (адрес) | **P** = [2, 1, 3] |
